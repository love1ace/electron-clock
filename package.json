{
  "name": "clock",
  "productName": "Clock",
  "version": "1.0.0",
  "description": "That I made to use",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds"
  },
  "dependencies": {
    "electron": "^13.1.7"
  },
  "devDependencies": {
    "electron-packager": "^15.3.0"
  }
}